# Test / local-dev dependencies for the shared `mami_core` algorithms.
# The services install their own deps inside their Docker images
# (see services/*/requirements.txt). Run the stack with: docker compose up --build
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pytest>=7.0.0
